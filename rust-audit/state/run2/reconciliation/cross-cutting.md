# Reconciliation (run 2) — cross-cutting

Agent REC-XC. Audited commit `fe9e84cc59b65367b31d5a3121774383cc422234` (`crates/core` and `crates/validator` byte-identical to `3ec8bc5`, where run 2's Phases 0–3 ran; `Cargo.lock` differs by two engine-only lines, `state/run2/baseline-delta.md`). Run 1 audited `2893917`. Scope of this part: `findings/F2-XC-001..009`, `F2-XC-050` (run 2) and `findings/F-XC-001/002/003/004/006/007/008/009/011/050/051` (run 1; `F-XC-005/010/052` left with the engine). Every `path:line` below was re-opened at `fe9e84c` in this session unless marked as quoted from a finding file; dependency lines are from the pinned registry sources under `~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/`.

Canonical rule applied: prefer the run-1 ID where both runs have the defect; the run with executed evidence carries the number; a run-2 ID is canonical only where run 1 has no counterpart. The Critics' duplicate clusters are canonicalised here: `F2-XC-001` over `F2-VAL-060`; `F2-XC-002` over `F2-VAL-065`; `F2-XC-003` (itself folded into `F-XC-004`) over `F2-VAL-068` and `F2-SEN-009` bullet 4; `F2-XC-007` over `F2-SEN-009` bullet 2; `F2-XC-008` (folded into `F-XC-009`) over `F2-SEN-009` bullet 1; `F2-XC-050` over `F2-CORE-036`.

## 1. Mapping of every run-2 cross-cutting finding

| F2 → F1 | Relation | Run 1 (final) | Run 2 (final) | Number the report carries, and why |
| --- | --- | --- | --- | --- |
| `F2-XC-001` → none | **NEW** | — (grep of all `F-*.md` for `toml::de::Error`, `input: Some`, `Termination`: only `F-CORE-030`'s remediation text mentions `main -> Result<_, Box<dyn Error>>`, for exit status, never for the payload) | High 95 E1 (five executions on both binaries: R8, C2-XC, R6, QA2-VAL-B, QA2-XC; `poc/F2-XC-001/`) | **High 95, canonical `F2-XC-001`** (folds `F2-VAL-060`, High 95). The most consequential run-2 addition in this area: a signer-key-to-stderr leak on any TOML error that run 1 did not find. |
| `F2-XC-002` → none | **NEW** | — (no `mode=rwc` / `create_if_missing` anywhere in run 1) | Low 92 E1 (`poc/F2-XC-002/`; sqlx-sqlite 0.9.0 `options/mod.rs:200`, `parse.rs:44-56`) | **Low 92, canonical `F2-XC-002`** (folds `F2-VAL-065`, Low 88). |
| `F2-XC-003` → `F-XC-004` | **CONFIRMS** | Low 85 E2 (Confirmed, QA reproduced by inspection) | Low 82 E2 (Confirmed) | **Low 85, canonical `F-XC-004`.** Same defect (no `USER`, floating tags, dropped `COMMIT_SHA`, no `HEALTHCHECK`); run 2 adds no `rust-toolchain.toml` and `--version` printing only `CARGO_PKG_VERSION` (`crates/validator/src/main.rs:35-38`); run 1 adds the GLIBC-skew failure mode and the `contracts/Dockerfile` `--chown` contrast. Both E2; run 1's number stands. Folds `F2-VAL-068` (Informational 90 — its Informational is a severity gap with both XC files' Low; Low kept because the root process owns the plaintext secret store) and `F2-SEN-009` bullet 4; run-1 `F-VAL-064`'s root-container sub-claim belongs here too (REC-VAL keeps its exit-0/`/health` claims). |
| `F2-XC-004` → `F-XC-011`, `F-XC-007` | **EXTENDS** | `F-XC-011` Low 95 E1 (advisory triage; Low set by h2 on the engine API); `F-XC-007` Informational 92 E1 (no CI gate; item 2 `sqlx-mysql/postgres` refuted in run 1) | Informational 85 E1 (five advisories incl. `rustls`, `lru`/`event-listener` warnings, `alloy` `full` and `push-gateway` feature width, duplicate crypto stacks; `poc/F2-XC-004/`) | **Informational; `F-XC-011` 90 and `F-XC-007` 92 as separate canonical rows.** No contradiction after re-running `cargo tree -i` myself at `fe9e84c` (below). Severity gap settled by scope: run 1's only reachable surface was the engine's axum API, which is out of scope under Section 11, and both runs executed that the metrics listener is HTTP/1-only. Run 2 extends run 1 with the `rustls` row (advisory published after run 1's database), the `alloy` `full`/`c-kzg`/`blst` and `push-gateway` width, and QA's `lru` correction (`alloy-provider` pins `0.16`; fix is `0.18.2`). |
| `F2-XC-005` → none | **NEW** | — (run 1 never examined `secure_delete`; its Phase 5 recorded `journal_mode = delete` as a concurrency fact in `F-VAL-035/038/066`; `F-VAL-035` leg (c) is the `foreign_keys` pragma, a different pragma; `SharingState` appears in run 1 only in DKG mechanics, `F-VAL-002/003/060`, never as a snapshot-retention gap) | Informational 90 E1, one `H` row corrected (bundled SQLite 3.51.3 without `SQLITE_SECURE_DELETE`; `poc/F2-XC-005/`) | **Informational 90, canonical `F2-XC-005`.** The corrected mechanism (both binaries statically link `libsqlite3-sys` `bundled`, so erasure is off in every image and the Dockerfiles' `libsqlite3` packages and comment are stale) is the version the report carries. |
| `F2-XC-006` → `F-XC-006` | **CONFIRMS** | Low 78 E2 (Confirmed; Gnosis↔Sepolia named as the unguarded pair) | Low 90 E1 (two local Anvil chains 100 → 11155111; request re-signed and mined on chain B; `poc/F2-XC-006/`) | **Low 90, canonical `F-XC-006`.** Run 2's PoC executes exactly the pair run 1 predicted: `BlockTime::Auto` still resolves only 100 and 11155111 (`crates/core/src/index/blocks.rs:37-41`), so the swap starts. Run 1 keeps the `get_chain_id` override and the five-table count; run 2 adds that `signer`/watched addresses are never compared either. A17 removes run 1's "restore into a differently-configured host" trigger, not the config-edit trigger (section 3). Related, not merged: `F-CORE-065` / `F2-CORE-066` (REC-CORE). |
| `F2-XC-007` → none | **NEW** | — (run 1 `F-XC-009` row 12 records analysis lead SEN-H15 and "left with R7"; no run-1 file was ever written) | Informational 75 E2 (Confirmed) | **Informational 75, canonical `F2-XC-007`** (folds `F2-SEN-009` bullet 2). A run-1 deferral that was never picked up, now filed. |
| `F2-XC-008` → `F-XC-009` | **EXTENDS** | Low 72 E2 (Confirmed for items 1/3/4, item 2 `0.0.0.0` refuted; items 3/4 canonical in `F-VAL-063`) | Informational 75 E2 (`known` for the zero-address TODO) | **Low 75, canonical `F-XC-009`.** Same core (both samples ship parseable key `0x…01`); run 2 adds the live `rpc.gnosischain.com` in both samples and the zero-address acceptance (validator fails loudly on `getCoordinator()`, sentinel idles), plus `voting_window = 0`. Run 2 does not repeat run 1's refuted item 2. Severity gap settled to **Low**: both runs verified the distinguishing asymmetry — the key placeholder parses and the service starts and signs while the adjacent zero address fails loudly — which is a "configuration weakness with limited impact", not documentation; the `known` tag applies to the zero-address sub-item only. Certainty 75 from run 2 because run 1's 72 was a compound over four items of which one was refuted. Folds `F2-SEN-009` bullet 1; `genesis_salt`/`participants` stay with `F-VAL-063` (REC-VAL). |
| `F2-XC-009` → `F-XC-001` | **CONFIRMS** | Informational 93 E1 (V-XC read `-C debug-assertions=off` and no `-C overflow-checks` off the release rustc invocations; `panic = "unwind"`) | Informational 90 E1 (same site panics in dev, wraps to `timestamp_ms: 2384` in release; `poc/F2-XC-009/`) | **Informational 93, canonical `F-XC-001`.** Both executed, same conclusion; run 1 shows the flags, run 2 shows the wrap. Both Critics rule the `u64::MAX/1000` header outside A4. Run 2 lists five timestamp sites to run 1's three (`blocks.rs:414,427,525,540-541,549`). |
| `F2-XC-050` → none | **NEW** | — (run 1 `F-CORE-067`/`F-SEN-001` state the "retains more than one snapshot" condition for the restart `Uncle` without noticing the single-row loss; `F-CORE-031` is the effect-side rollback loss, counterpart of `F2-CORE-030`, REC-CORE) | Low 92 E1 (fault-injected enqueue after commit+prune on a warp page and at depth 0; depth-2 control recovers; sentinel `Claim` loss executed; `poc/F2-XC-050/`) | **Low 92, canonical `F2-XC-050`** (folds `F2-CORE-036`, Low 92; fold in its `start_block` fresh-start variant and "prune after `queue`" note, which QA showed does not cover the empty-store first page). |

Counts: CONFIRMS 3 (`F2-XC-003/006/009`), EXTENDS 2 (`F2-XC-004/008`), NEW 5 (`F2-XC-001/002/005/007/050`), CONTRADICTS 0.

### Advisory reachability re-run (settles the only apparent disagreement)

Run 1 (`F-XC-011`) analysed `quinn-proto`, `h2`, `ruint`, `crossbeam-epoch`; run 2 (`F2-XC-004`) those plus `rustls`. Run 2's reviewer text said h2 is reachable through the metrics listener; run 2's Critic corrected that to "linked, no connection path"; run 1 had executed the same conclusion for the metrics listener (0-byte reply to an HTTP/2 preface) and found the only HTTP/2 server in the engine. Re-run in this session at `fe9e84c` with `cargo tree --offline --locked -p validator -p sentinel`:

| Crate | `cargo tree -i` (this session) | Conclusion carried |
| --- | --- | --- |
| `h2 0.4.14` | `hyper 1.10.1` ← `hyper-rustls`/`hyper-util`/`metrics-exporter-prometheus`/`reqwest` | Linked; no connection path in scope: `metrics-exporter-prometheus-0.18.3/src/exporter/http_listener.rs:9,77,122` uses `server::conn::http1::Builder` on both listener paths; reqwest's activated features are `__rustls`, `__rustls-aws-lc-rs`, `__tls`, `json`, `rustls` (no `http2`). Informational. |
| `quinn-proto 0.11.14` | `warning: nothing to print.` | Not compiled. Lockfile-only. |
| `rustls 0.23.40` | `alloy-transport-ws`, `hyper-rustls` (exporter, reqwest), `rustls-platform-verifier` | Informational (advisory: transcript stays authenticated). New since run 1. |
| `ruint 1.18.0` | `alloy-primitives 1.6.0` → alloy | Informational; only affected calls are `Signed` shifts, unused in scope (both runs). |
| `crossbeam-epoch 0.9.18` | `crossbeam-deque` ← `rayon`; `metrics-util` ← exporter | Informational (both runs). |
| `lru 0.16.4` / `event-listener 5.4.1` | `alloy-provider`; `sqlx-core` | Informational; `lru` not fixable by `cargo update` (run 2 QA). |

No contradiction between the runs' executed evidence. Two run-1 items that changed status with scope: the h2 Low (engine API) and the "spin 0.9.8 reaches all three binaries" line (now two).

## 2. Run-1 cross-cutting findings run 2 did not rediscover

| Run-1 ID | Status at `fe9e84c` | Detail |
| --- | --- | --- |
| `F-XC-002` (Debug-derived secret types reach log sinks; Low 88) | **Still valid — miss (soft).** | `crates/validator/src/service/effect.rs:270` still `tracing::warn!(?effect, %err, …)`; `Secrets` (`frost/keygen.rs:27-28`) and `KeyShare` (`:434-435`) still plain derives; core sinks at `effects.rs:62,66,90`, `driver.rs:267`. Run 2's C2-XC checked frost-core's redaction and dismissed the leak (`F2-XC-007` Critic, rejected-list check) — the same result run 1's V-VAL executed — but filed nothing for the hygiene gap run 1 kept (no in-tree test pins the upstream `Debug`; core's `Debug` bound delegates safety downstream). Per run 1's canonical split the defect is counted under `F-VAL-062` (validator) and `F-CORE-036` (core), REC-VAL/REC-CORE; this file stays the union. |
| `F-XC-003` (`deny_unknown_fields` + `flatten`; Informational 96, test gap) | **Still valid as a test gap — residual not filed by run 2.** | Run 2 re-executed the behaviour and found it correct (`F2-XC-008` Considered-and-rejected, `r8-config-tests.txt`), matching run 1's six passing tests; neither run's tests are in the tree (`grep -i unknown crates/{validator,sentinel}/src/config.rs`: attribute lines only). Not a defect miss; the Informational test-gap row stands. |
| `F-XC-008` (unconfigured `reqwest` clients; Low 80 → **Informational 80**) | **Item 1 out of scope (engine); items 2/3 rediscovered by run 2 in `F2-SEN-008` and settled here from the pinned source.** | `reqwest-0.13.4/src/async_impl/client.rs:309-310` (`auto_sys_proxy: true`, `redirect_policy: redirect::Policy::default()`), `:416` (`proxies.push(ProxyMatcher::system())`), `redirect.rs:161-163` (`Policy::limited(10)`), `hyper-util-0.1.20/src/client/proxy/matcher.rs:240` (`from_system` always calls `from_env`), `:231-232` (`ALL_PROXY`, `HTTP_PROXY`). The `system-proxy` feature (off in this build, as `F2-XC-004` notes) only adds macOS/Windows platform lookups; environment proxies are honoured regardless. Run 1's `I` for items 2/3 becomes E2. With the CoW client gone from scope and the engine trusted under A3, the residual (sentinel → engine hop follows redirects and honours proxy env) is Informational. Not a miss: `F2-SEN-008`'s Claim states both defaults (REC-SEN's row; cross-reference only). |
| `F-XC-050` (no DKG membership check; pure-cardinality close; silent `None` finalisation; Medium 48 Plausible) | **Still valid — miss.** | `crates/validator/src/state/keygen.rs:448-449` unchanged (`confirmations.insert(event.participant); if confirmations.len() as u16 != count`), no `group.participants` consultation; genesis branch passes `status.key_share()` at `:482`; `finalize_key_gen` (`:1295-1327`) is silent on `None`. Run 2's `F2-VAL-061` (High 90, QA'd) is the precondition run 1 called `F-VAL-060` (50%) — so run 2 strengthened the precondition and did not file its DKG consequence. C-VAL-A's window correction stands: `KeyGenConfirmation::Collecting` persists only when a complaint is outstanding (`:383-405`). Certainty kept at 48: the precondition rose, the window evidence did not move. A16 split in section 3. |
| `F-XC-051` (`verify_commitment` delegates structural checks; Low 42 Plausible → **Informational 85**) | **Miss, then settled in the code's favour.** | The finding's one `I` leg ("what frost-core does with an empty or identity commitment") is readable now. Empty `c`: `frost-core-3.0.0/src/keys/dkg.rs:458` `let phi_ell0 = commitment.verifying_key()?;` → `keys.rs:373-376` `self.0.first().ok_or(Error::MissingCommitment)?` — an `Err`, not a panic. Identity `c[0]`: `dkg.rs:412` serialises the verifying key inside `challenge`, and `frost-secp256k1-3.0.0/src/lib.rs:119-122` returns `Err(GroupError::InvalidIdentityElement)` for the identity — so the proof of knowledge is **rejected**, not trivially satisfied; the finding's "degenerates to `R = g^mu`" claim does not hold for this ciphersuite. Both halves therefore land on the first row of the finding's own outcome table ("`Err` … vindicates the comment … drops the finding to Informational"). Residual: a short non-empty `c` under the `F-VAL-060`/`F2-VAL-061` precondition was not traced; the three-line decode-site validation (option 1) remains cheap hardening. The run-1 PoC (`poc/F-XC-051/`) stays unrun (this pass edits no tracked file); E2 from pinned source, 85. |

Not counted as misses: `F-XC-001/004/006/007/009/011` were all rediscovered (section 1); `F-XC-005/010/052` were engine findings removed with the scope.

## 3. A16 / A17 applied to run-1 cross-cutting findings

| Run-1 ID | Change | Justification |
| --- | --- | --- |
| `F-XC-050` | **Split.** Genesis instance → Informational, `known` (A16). Rollover instance keeps **Medium / Plausible 48**. Header severity stays Medium. | The title's consequence ("silently finalises genesis") is genesis-ceremony liveness only, which A16 reads as Informational. But the same cardinality close feeds the non-genesis branch: `keygen.rs:512` `let key_share = status.key_share();` is carried into `RolloverState::SigningRollover { key_share, … }` (`:552-557`) and, after `EpochStaged`, into `finalize_key_gen` (`:599`), where `None` again records no epoch and emits nothing. A rollover key generation closed early by injected confirmations therefore loses epoch `k+1` for that validator — reaching later epochs, so A16 does not lower it. The in-code comment at `:451-452` ("non-genesis confirmation whose rollover-packet branch isn't wired in yet") is stale; the branch is wired. |
| `F-XC-006` | **Trigger narrowed, severity unchanged (Low).** | Run 1's second trigger, "restoring a database into a differently-configured host", requires out-of-band database access and is out of scope under A17. The first trigger (editing `rpc`/`consensus`/`signer` with the same `database`) needs no database access and is the one run 2 executed. `F-CORE-065`'s queue-specific half likewise (REC-CORE). |
| `F-XC-001/002/003/004/007/008/009/011/051` | **No change.** | None depends on genesis-only liveness or on out-of-band database access. `F-XC-051`'s status change (section 2) comes from evidence, not from an assumption. |

## 4. Team dispositions touching this area

None. `state/pr-review-threads.md` lists 13 threads on `F-ENG-030`, `F-VAL-005`, `F-SEN-002/015/005/003`, `F-VAL-004/032/033`, `F-CORE-067/033`; no `F-XC-*` finding was commented on, accepted, assigned or claimed fixed. For completeness: `report/KNOWN-WORK.md` maps `F-XC-002` to issue #113 (TRACKED BUT UNDERSTATED) and `report/IN-FLIGHT.md` recorded `F-XC-002` unchanged by the pruning stack (now merged; confirmed above at `effect.rs:270`) and `F-XC-009` unchanged by the unmerged batex stack — assessments, not dispositions.

## 5. Ledger rows — every cross-cutting finding in either run

| Canonical ID | Final severity | Final certainty | Basis | Status | Trail | Team |
| --- | --- | --- | --- | --- | --- | --- |
| `F-XC-001` | Informational | 93 | E1 | Confirmed | run 1 I→E1 93 (release flags read off rustc); run 2 `F2-XC-009` E1 90 confirms (dev panic / release wrap). | — |
| `F-XC-002` | Low | 88 | E1 (leak refuted) / E2 (hygiene) | Verified (reduced); union row, counted under `F-VAL-062` + `F-CORE-036` | run 1 E2 74 → V-VAL 88 (secrets redacted upstream, hygiene gap kept); run 2 checked the redaction, filed nothing — miss of the hygiene gap. | #113 (KNOWN-WORK) |
| `F-XC-003` | Informational | 96 | E1 | Verified (test gap) | run 1 I 58 → V-XC 96 (six tests pass); run 2 re-executed the same behaviour as a rejected hypothesis; no in-tree test at `fe9e84c`. | — |
| `F-XC-004` | Low | 85 | E2 | Confirmed; canonical over `F2-XC-003`, `F2-VAL-068`, `F2-SEN-009` b4, `F-VAL-064` root sub-claim | run 1 E2 85; run 2 `F2-XC-003` E2 82 confirms (+ no toolchain pin, `--version`). | — |
| `F-XC-006` | Low | 90 | E1 | Confirmed; canonical over `F2-XC-006` | run 1 E2 78; run 2 `F2-XC-006` E1 90 (two Anvil chains) confirms; A17 drops the restore trigger only. | — |
| `F-XC-007` | Informational | 92 | E1 | Confirmed (item 2 refuted in run 1) | run 1 E2 84 → V-XC 92 (`cargo audit` exits 1); run 2 `F2-XC-004` extends (feature width, duplicate stacks, CI gate). | — |
| `F-XC-008` | Informational (was Low) | 80 | E2 (items 2/3 settled from pinned reqwest/hyper-util) | Confirmed, narrowed; item 1 out of scope (engine) | run 1 E2/I 80 → V-XC 94 on item 1 (engine); items 2/3 now E2 (this pass); run 2 `F2-SEN-008` restates them for the sentinel. | — |
| `F-XC-009` | Low | 75 | E2 | Confirmed (item 2 refuted in run 1); canonical over `F2-XC-008`, `F2-SEN-009` b1; items 3/4 under `F-VAL-063` | run 1 E2 72 (compound); run 2 `F2-XC-008` E2 75 extends (live RPC, zero-address acceptance, `voting_window` 0); severity settled Low over run 2's Informational. | — |
| `F-XC-011` | Informational (was Low) | 90 | E1 | Confirmed; canonical for advisory reachability over `F2-XC-004`'s table | run 1 E1 95 (Low via engine API h2); engine out of scope → Informational; run 2 E1 85 adds `rustls`, `lru` fix note; `cargo tree -i` re-run here agrees. | — |
| `F-XC-050` | Medium (rollover) / Informational `known` (genesis, A16) | 48 | E2 | Plausible; **run 2 miss** | run 1 E2 48 (C-VAL-A window correction); run 2 `F2-VAL-061` High 90 strengthens the precondition but files no consequence; code unchanged at `fe9e84c`. | — |
| `F-XC-051` | Informational (was Low) | 85 | E2 (pinned frost-core / frost-secp256k1) | Settled in the code's favour; **run 2 miss** | run 1 E2/I 42; this pass: empty `c` → `Err(MissingCommitment)`, identity `c[0]` → `Err(InvalidIdentityElement)` inside `challenge`; hardening option 1 stands. | — |
| `F2-XC-001` | High | 95 | E1 | Confirmed; **NEW** (run 1 miss); canonical over `F2-VAL-060` | run 2 R8 E1 90 → C2-XC High 95 → QA2-XC 95 (five executions); no run-1 counterpart. | — |
| `F2-XC-002` | Low | 92 | E1 | Confirmed; **NEW**; canonical over `F2-VAL-065` | run 2 E1 90 → 92; no run-1 counterpart. | — |
| `F2-XC-005` | Informational | 90 | E1 (one `H` row corrected) | Confirmed; **NEW** | run 2 E2 75 → QA2-XC E1 90 with the bundled-SQLite correction; run 1 never examined `secure_delete`. | — |
| `F2-XC-007` | Informational | 75 | E2 | Confirmed; **NEW**; canonical over `F2-SEN-009` b2 | run 2 E2 75; run 1 lead SEN-H15 recorded in `F-XC-009` row 12, never filed. | — |
| `F2-XC-050` | Low | 92 | E1 | Confirmed; **NEW**; canonical over `F2-CORE-036` | run 2 Coverage Critic 55 → C2-XC 72 → QA2-XC E1 92; no run-1 counterpart. | — |
| `F2-XC-003` | Low | — | — | Folded into `F-XC-004` | see `F-XC-004`. | — |
| `F2-XC-004` | Informational | — | — | Folded into `F-XC-011` (advisories) and `F-XC-007` (gate, surface) | see those rows. | — |
| `F2-XC-006` | Low | — | — | Folded into `F-XC-006` | see `F-XC-006`. | — |
| `F2-XC-008` | Low | — | — | Folded into `F-XC-009` (items 3/4: `F-VAL-063`) | see `F-XC-009`. | — |
| `F2-XC-009` | Informational | — | — | Folded into `F-XC-001` | see `F-XC-001`. | — |
| `F2-VAL-060` | High | — | — | Folded into `F2-XC-001` (REC-VAL writes the row) | validator instance, E1 95. | — |
| `F2-VAL-065` | Low | — | — | Folded into `F2-XC-002` (REC-VAL) | validator instance, E1 88. | — |
| `F2-VAL-068` | Low (as folded) | — | — | Folded into `F2-XC-003` → `F-XC-004` (REC-VAL) | validator instance, E2 90, filed Informational. | — |
| `F2-SEN-009` b1 / b2 / b4 | Low / Informational / Low (as folded) | — | — | Folded into `F-XC-009` / `F2-XC-007` / `F-XC-004` (REC-SEN keeps bullet 3, stale `Preparing` docs) | sentinel instances, E2 85. | — |
| `F2-CORE-036` | Low | — | — | Folded into `F2-XC-050` (REC-CORE) | core promotion, E1 92 mirrored. | — |
| `F-XC-005`, `F-XC-010`, `F-XC-052` | — | — | — | Out of scope (engine removed, Section 11) | removed from `findings/` before run 2. | — |

Canonical distinct cross-cutting defects: **16 rows, 15 counted** (`F-XC-002` is a union counted under `F-VAL-062`/`F-CORE-036`). By severity (counted rows): High 1 (`F2-XC-001`), Medium 1 (`F-XC-050`, rollover instance), Low 5 (`F-XC-004`, `F-XC-006`, `F-XC-009`, `F2-XC-002`, `F2-XC-050`), Informational 8 (`F-XC-001`, `F-XC-003`, `F-XC-007`, `F-XC-008`, `F-XC-011`, `F-XC-051`, `F2-XC-005`, `F2-XC-007`). By status: Confirmed 12, Plausible 1 (`F-XC-050`), Verified-reduced/settled 2 (`F-XC-002` union, `F-XC-051`), test gap 1 (`F-XC-003`). Certainty bands: 90–100 ×9, 70–89 ×5, 40–69 ×1.

Severity moves recorded in this part: `F-XC-008` Low → Informational (scope), `F-XC-011` Low → Informational (scope), `F-XC-051` Low → Informational (evidence), `F-XC-050` genesis instance → Informational `known` (A16), `F2-XC-008` Informational → Low (settled to the run-1 canonical). Certainty moves: `F-XC-006` 78 → 90 (run-2 E1), `F-XC-051` 42 → 85 (pinned source), `F-XC-008` items 2/3 I → E2.
